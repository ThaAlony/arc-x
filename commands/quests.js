/*exports.run = (client, message, args, command, account) => {

    if (!args[0]) {
        if (lingua == "it") return message.reply(" l'utilizzo corretto � " + client.config.prefix + command + " lista/completate ")
        return message.reply(" the correct use is " + client.config.prefix + command + " list/completed ")
    }
    switch (args[0].toLowerCase()) {
        case "list":
            client.questList(client,message, lingua)
            break;
        case "lista":
            client.questList(client,message, lingua)
            break;
    }

}*/